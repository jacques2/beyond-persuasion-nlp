"""Integration tests for the end-to-end guarded agent.

Suggested checks:
- the protected prompt is used when vulnerability is detected,
- guarded and unguarded outputs are stored separately,
- orchestration errors fail loudly and predictably.
"""
